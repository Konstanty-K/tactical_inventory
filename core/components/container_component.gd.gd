class_name ContainerComponent
extends ItemComponent

# Przechowuje własną, unikalną instancję bazy danych siatki
@export var inventory: InventoryData

# UWAGA: W przyszłości można tu dodać sloty wewnętrzne (np. zintegrowana kabura w plecaku)
